{
  "name": "triserv-site",
  "version": "1.0.0",
  "description": "Trident Services Commercial Site",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:components": "parcel build src/*.js",
    "build:html": "parcel build pages/*.html",
    "server": "node server.js",
    "parcel:clean": "rm -rf .parcel-cache dist"
  },
  "author": "Sarah",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "lit": "^3.1.3",
    "parcel": "^2.12.0",
    "pg": "^8.11.5"
  }
}
